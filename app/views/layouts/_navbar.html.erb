<nav class="navbar is-black" role="navigation" aria-label="main navigation">
  <div class="container">
    <div class="navbar-brand">
      <div class="navbar-item">
        <span class="title is-4 has-text-white">Tic tac toe game</span>
      </div>
    </div>

    <% if @current_user%>
      <div class="navbar-end">
        <div class="navbar-item">
          <span class="title is-4 has-text-white"><%= @current_user.username %></span>
        </div>

        <div class="navbar-item">
          <%= link_to 'Logout', logout_path, class: 'level-item button is-small is-danger', method: :delete %>
        </div>
      </div>
    <% end %>
  </div>
</nav>
